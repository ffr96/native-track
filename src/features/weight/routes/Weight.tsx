import { Box } from '@/components/Layout/Box';
import { DisplayWeights } from '../components/DisplayWeights';

export const Weight = () => {
  return (
    <Box style={{ height: '84%' }}>
      <DisplayWeights />
    </Box>
  );
};
