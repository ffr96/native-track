import { ViewForm } from '../components/ViewForm';
import { LoginForm } from '../components/LoginForm';

export const Login = () => {
  return (
    <ViewForm>
      <LoginForm />
    </ViewForm>
  );
};
